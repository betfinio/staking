import cz from '../translations/cz.json';
import en from '../translations/en.json';
import staking from '../translations/en/staking.json';
import ru from '../translations/ru.json';

const resources = {
	cz,
	en,
	ru,
	staking,
} as const;

export default resources;
