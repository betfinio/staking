# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting Staking Production
'on':
  push:
    branches:
      - master
env:
  PUBLIC_ETHSCAN: ${{ vars.PUBLIC_ETHSCAN }}
  PUBLIC_TOKEN_ADDRESS: ${{ vars.PUBLIC_TOKEN_ADDRESS }}
  PUBLIC_CONSERVATIVE_STAKING_ADDRESS: ${{ vars.PUBLIC_CONSERVATIVE_STAKING_ADDRESS }}
  PUBLIC_PASS_ADDRESS: ${{ vars.PUBLIC_PASS_ADDRESS }}
  PUBLIC_AFFILIATE_ADDRESS: ${{ vars.PUBLIC_AFFILIATE_ADDRESS }}
  PUBLIC_BTCUSDT_FEED_ADDRESS: ${{ vars.PUBLIC_BTCUSDT_FEED_ADDRESS }}
  PUBLIC_BTCUSDT_GAME_ADDRESS: ${{ vars.PUBLIC_BTCUSDT_GAME_ADDRESS }}
  PUBLIC_CORE_ADDRESS: ${{ vars.PUBLIC_CORE_ADDRESS }}
  PUBLIC_PARTNER_ADDRESS: ${{ vars.PUBLIC_PARTNER_ADDRESS }}
  PUBLIC_BETS_MEMORY_ADDRESS: ${{ vars.PUBLIC_BETS_MEMORY_ADDRESS }}
  PUBLIC_DYNAMIC_STAKING_ADDRESS: ${{ vars.PUBLIC_DYNAMIC_STAKING_ADDRESS }}
  PUBLIC_ENVIRONMENT: ${{ vars.PUBLIC_ENVIRONMENT }}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          registry-url: "https://npm.pkg.github.com"
          scope: '@betfinio'
          bun-version: latest
      - run: bun install
        env:
          BUN_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
      - run: bun run build --env-mode production
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BETFIN_DEV }}'
          channelId: live
          projectId: betfin-dev
          target: staking
